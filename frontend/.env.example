# Backend API URL (leave as /api for Docker; set full URL for separate deployments)
VITE_API_URL=/api

# Deployed smart contract address on Ethereum Sepolia (optional — Web3 features disabled if blank)
VITE_CONTRACT_ADDRESS=
