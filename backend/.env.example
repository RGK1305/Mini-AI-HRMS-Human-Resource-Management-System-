# Database
DATABASE_URL=postgresql://rize_user:rize_pass@db:5432/rize_os

# JWT — keep this strong and secret
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# AI Provider
AI_PROVIDER=openai
AI_API_KEY=

# Web3 — Ethereum Sepolia (for Hardhat deployment only, not runtime)
SEPOLIA_RPC_URL=https://rpc.sepolia.org
DEPLOYER_PRIVATE_KEY=

# ─── Org Cryptography (server-only — NEVER prefix with VITE_) ─────────────────
# These values are used by POST /api/tasks/:id/generate-hash to compute
# the salted keccak256 hashes that go on-chain. They never reach the browser.

# The organisation name — hashed to produce the on-chain orgId (bytes32).
# Must be consistent across deployments for auditing to work.
ORG_NAME=My Company

# The secret salt — combined with task data before hashing.
# Treat this like a password. Anyone with this can re-derive on-chain records.
ORG_SECRET=change-this-to-a-strong-random-secret

# CORS — comma-separated list of allowed frontend origins in production
# Leave blank for local dev (defaults to localhost:5173 and localhost:3000)
# e.g. CORS_ORIGIN=https://rizeos.com,https://www.rizeos.com
CORS_ORIGIN=
