# Database
DATABASE_URL=postgresql://rize_user:rize_pass@db:5432/rize_os

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# AI API (Claude or OpenAI) — leave blank for mock fallback
AI_PROVIDER=openai
AI_API_KEY=

# Web3 — Ethereum Sepolia Testnet
SEPOLIA_RPC_URL=https://rpc.sepolia.org
DEPLOYER_PRIVATE_KEY=

# Frontend (VITE_ prefix = exposed to the browser bundle)
VITE_API_URL=http://localhost:5000/api

# Deployed WorkforceLogger contract address on Sepolia
VITE_CONTRACT_ADDRESS=0xF82Fa39323c68F13125B3F8278028614C13fF537

# NOTE: ORG_SECRET and ORG_NAME are backend-only variables.
# They are deliberately NOT prefixed with VITE_ so they never
# end up in the browser bundle. See backend/.env.example.
